# Abhängigkeiten des Video-Automatisierungs-Workers (video_pipeline/).
#
# Bewusst NICHT Teil von requirements.txt: Der Telegram-Bot läuft leicht
# auf Railway (nixpacks.toml), der Video-Worker braucht dagegen ffmpeg,
# OpenCV und deutlich mehr Rechenleistung (siehe docs/video-automation/
# README.md für den separaten Deploy, z. B. GPU-VM/Modal/RunPod).
boto3==1.35.36
opencv-python-headless==4.10.0.84
numpy==1.26.4
scenedetect[opencv]==0.6.4
ImageHash==4.3.1
Pillow==10.4.0
google-genai==0.7.0
anthropic==0.34.2
faster-whisper==1.0.3
fastapi==0.115.0
uvicorn[standard]==0.31.0
python-telegram-bot==20.7
requests==2.32.3
